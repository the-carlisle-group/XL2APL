 GetWorkbookInfo←{
     ⍝ ⍵ ←→ FQP of .xlsx file
     ⍝   ←→ Space of usefull stuff
     a←OpenExcelFile ⍵
     p←⎕NS''
     p.FileName←⍵
     p.(SheetNames SheetIds)←a GetSheetNames''
     e←⌷a.Entries
     n←e.FullName
     b←∨/¨'xl/worksheets/sheet'∘⍷¨n
     s←b/e
     p.SheetFileNames←s.FullName
     p.Lengths←Convert.ToInt32¨s.Length
     p.CompressedLengths←Convert.ToInt32¨s.CompressedLength
     _←Dispose a
     p
 }
