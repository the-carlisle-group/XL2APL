 ReadZipFile←{
     ⍝ ⍺ ←→ .NET ZipArchive
     ⍝ ⍵ ←→ .NET Entry FullName
     ⎕USING←'System'
     d←('/'∊⍕⌷⍺.Entries)⊃'\/'
     e←⍺.GetEntry⊂d@('/'∘=)⍵
     e=⎕NULL:''
     l←Convert.ToInt32 e.Length
     s←e.Open
     b←Array.CreateInstance Byte l
     _←s.Read b 0(l)
     _←Dispose s
     'UTF-8'⎕UCS b.Clone
 }
