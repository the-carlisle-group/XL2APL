 GetSheetNames←{
     ⍝ ⍺ ←→ Excel ZipArchive
     g←{0=≢⍵:'' ⋄ ⍺∘{(⍵[;0]⍳⊂⍺)⊃⍵[;1]}¨⍵}

     f←'xl/workbook.xml'
     t←⍺ ReadZipFile f
     m←⎕XML t
     k←⍸m[;1]∊⊂'sheet'
     a←m[k;3]
     n←'name'g a
     i←'sheetId'g a

     ⍝ Named Ranges (Might be none)
     k←⍸m[;1]∊⊂'definedName'
     rn←'name'g m[k;3]
     ra←m[k;2]             ⍝ Address

     n i rn ra
 }
