 GetSheetNames←{
     ⍝ ⍺ ←→ Excel ZipArchive
     g←{(⍵[;0]⍳⊂⍺)⊃⍵[;1]}
     f←'xl/workbook.xml'
     t←⍺ ReadZipFile f
     m←⎕XML t
     k←⍸m[;1]∊⊂'sheet'
     a←m[k;3]
     n←'name'∘g¨a
     i←'sheetId'∘g¨a
     n i
 }
